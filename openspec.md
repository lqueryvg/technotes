# OpenSpec

```
cd your-project
openspec init
openspec update

cat <<EOF > .claude/CLAUDE.md
When working in an OpenSpec project (i.e. an openspec/ directory exists),
update MEMORY.md with current progress every time you update tasks.md
(checking off tasks).
EOF

/opsx:new <what-you-want-to-build>
```
